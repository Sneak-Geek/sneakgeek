import React from 'react';
import {Button} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const HomeScreen = () => (
  <SafeAreaView style={{flex: 1}}>
    <Button title={'Go to shoe detail'} onPress={() => {}} />
  </SafeAreaView>
);

export default HomeScreen;
